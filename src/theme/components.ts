import { theme as Brand } from "components/App/Header/Brand"
import { theme as BrandIcon } from "components/App/shared/BrandIcon"
import { theme as RadioCard } from "components/App/Main/shared/RadioCard"

export const components = {
  Brand,
  BrandIcon,
  RadioCard
}
